name: flask-app
replicas: 2

LabelKey: app
LabelValue: flask-app

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0

containers:
  name: flask-app
  image: andry67/helloworld
  tag: latest
  imagePullPolicy: IfNotPresent
  port: 5000
  protocol: TCP
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
  readinessProbe:
    path: /
    initialDelaySeconds: 5
    periodSeconds: 10
    failureThreshold: 3
  livenessProbe:
    path: /
    initialDelaySeconds: 5
    periodSeconds: 10
    failureThreshold: 3

ingress-nginx:
  enabled: true
  controller:
    service:
      type: NodePort
    ingressClassResource:
      name: nginx
      default: true

Ingress:
  IngressClassName: nginx
  host: formazionesou.local
  path: /
  pathType: Prefix
  service:
    port: 80

ClusterIP:
  name: service-flask-app
  servicePort: 80
  targetPort: 5000

